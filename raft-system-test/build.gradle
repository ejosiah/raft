plugins {
    id 'java'
}

version 'unspecified'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile project(":raft-protocol")
    compile project(":raft-server")
    compile project(":raft-client")

    testCompile project(":test-support")
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
